import React from 'react';

const LoginForm = () => {
  return (
    <form>
      <input name="email" />
      <input type="password" name="password" />
    </form>
  );
};

export default LoginForm;
